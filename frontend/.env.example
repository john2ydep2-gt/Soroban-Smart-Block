# URL of the deployed PERO-J backend API (including /api path prefix)
# Example: https://api.example.com/api
# Leave empty to use the Vite dev proxy (http://localhost:3001/api) during local development
VITE_API_BASE_URL=
